#include <iostream>

using namespace std;

/*
Create a class BigInt to represent "arbitrarily large" integers. For the internal representation, use a vector of elements of an appropriate unsigned type and overload the operators +, -, *, / (integer division), % (modulo operation), and the output operator <<.
*/

int main() {

    return 0;
}

/*
Input:


Output:

*/