#include <iostream>

using namespace std;

/*
Write a function that takes a string as a parameter and returns a "scrambled" string.
The result should first contain all characters with an odd index in the parameter in the original order
and then all characters with an even index in the parameter in reverse order, e.g.:

Parameters: "abcdefghi"
Return value: "bdfhigeca"
Don't use a loop in the function.
*/

int main() {

    return 0;
}

/*
Input:


Output:

*/