#include <iostream>
#include <random>

using namespace std;

/*
Implement a Dictionary class that is used to store objects in such a way that they can be found again using a freely assignable key value. All entered objects must belong to a class Object. The following program should be possible:
object a,b;
dictionary dict; //empty dictionary
dict.add(a,"Object A");
dict.add(b,"Object B");
dict.search("Object C"); // not found
dict.search("Object A"); // -> returns the object corresponding to variable a
dict.remove("Object A"); // removes an entry
dict.search("Object A"); // not found because the entry was removed
A hash table is recommended for internal representation (Google!).
*/

int main() {

    return 0;
}

/*
Input:

Output:

*/