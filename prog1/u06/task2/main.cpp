#include <iostream>

using namespace std;

/*
Write a function that takes a vector of double values ​​as reference parameter and sorts the values ​​in this vector (use the Selection Sort method for this). The function should determine the following statistical values and return them to the calling function: sum of the values, mean value of the values, standard deviation, median and the other quartiles. (It is beneficial to define multiple helper functions, each performing a subtask.)
*/

int main() {

    return 0;
}

/*
Input:


Output:

*/