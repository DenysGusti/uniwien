#include <iostream>

using namespace std;

using ld = long double;

/*
Write the functions int mult(int m, int n) and int power(int m, unsigned int n) that compute the product 
m∗n and the exponential function m^n respectively of the integers m and n. Do not use the * (multiplication) operator for this. The use of any external routines (e.g. math libraries) is also not permitted! Note that int values ​​can also be negative or equal to 0.

Note: 
m∗n=m+m∗(n−1) for 
n>0 or 
m 
n
 =m∗m 
n−1
*/

int main() {
    
    return 0;
}

/*
Input:


Output:

*/