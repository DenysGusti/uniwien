#include <iostream>

using namespace std;

/*
Write the functions int mult(int m, int n) and int power(int m, unsigned int n) that calculate the product 
m∗n and the exponential function m^n of the integers m and n. Do not use the * operator (multiplication) or loops in the function.
The use of any external routines (e.g. math libraries) is also not allowed! Note that int values ​​can also be negative or equal to 0.
Note: m∗n=m+m∗(n−1) for n>0 or m^n=m∗m^(n−1)
*/

int main() {

    return 0;
}

/*
Input:


Output:

*/